interface:
  display_name: "Client Requirements To Plan"
  short_description: "Turn client briefs into saved build plans"
  default_prompt: "Use $client-requirements-to-plan to turn this client brief into a numbered, testable plan saved in the client folder."
