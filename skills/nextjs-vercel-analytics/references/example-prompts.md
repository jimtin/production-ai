# Example Prompts

- Use `$nextjs-vercel-analytics` to add Vercel Web Analytics tracking to this Next.js app.
- Use `$nextjs-vercel-analytics` to instrument signup and purchase flows with Vercel custom events.
- Use `$nextjs-vercel-analytics` to review this app's analytics payloads for PII leakage.
- Use `$nextjs-vercel-analytics` to connect frontend CTA events with backend successful mutation events.
- Use `$nextjs-vercel-analytics` to add typed analytics events to the admin portal without tracking user-identifying data.
